## Packages
framer-motion | For smooth page transitions and list animations
qrcode.react | To generate QR codes for session sharing
react-dropzone | For drag-and-drop file uploads
lucide-react | Iconography (already in base, but essential)
clsx | Utility for conditional classes
tailwind-merge | Utility for merging tailwind classes

## Notes
WebSocket connection required at /ws?code={sessionCode}
File uploads handled via standard POST to /api/sessions/:code/items
Monochromatic theme: pure black/white with subtle grays
